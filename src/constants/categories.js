/**
 * @deprecated Raccourci vers `@/lib/financeCategories` (source de vérité unique).
 * Les anciens import de `@/constants/categories` continuent de fonctionner.
 */
export {
  TRANSACTION_CATEGORIES,
  INCOME_CATEGORIES,
  EXPENSE_CATEGORIES,
  getCategoryType,
  FINANCE_CATEGORIES,
  resolveKey,
  labelOf,
} from '@/lib/financeCategories';